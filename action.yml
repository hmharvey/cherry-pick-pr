name: 'Cherry-pick PR'
description: 'cherry-pick commits from a PR'
author: 'hmharvey'
outputs:
  branch_name:
    description: 'new checked out branch'
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'

