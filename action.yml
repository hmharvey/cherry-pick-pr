name: 'Cherry-pick PR'
description: 'cherry-pick commits from a PR'
author: 'hmharvey'
outputs:
  branch_name:
    description: 'new checked out branch'
runs:
  using: 'docker'
  image: 'Dockerfile'

